# 搖骰子遊戲 — 技術設計文件（spec）

| 項目 | 內容 |
|---|---|
| 版本 | v1.1 |
| 日期 | 2026-08-14 |
| 對應需求 | requirements.md v1.2 |
| 範圍 | 描述實作邏輯與技術決策，不包含完整程式碼 |

### 修訂記錄
| 版本 | 變更 |
|---|---|
| v1.0 | 初版 |
| v1.1 | 對齊實作：① 對戰平手自動重擲流程（§3.4／§6）；② 面↔旋轉對照表修正（§3.2）；③ 狀態欄位對齊（§2）；④ 動畫必須 `fill:'forwards'` 之警示（§5.2）；⑤ 模式切換須重建骰子 DOM、`[hidden]` CSS 覆蓋注意（§6／§8）；⑥ 響應式桌機寬度（§9） |

---

## 1. 架構總覽

單一 `dice-game.html`，內部以三層分工，邏輯與畫面完全分離：

| 層 | 職責 | 對應區塊 |
|---|---|---|
| **狀態層（State）** | 維護全域狀態：目前模式、骰數、是否擲骰中、下注選擇、戰績、音效開關 | `<script>` 中單一狀態物件 |
| **引擎層（Engine）** | 產生隨機結果、判定勝負、控制音效播放，**不接觸 DOM** | 純函式集合 |
| **視圖層（View）** | 渲染骰子、結果區、計分板，播放動畫，接收事件 | DOM + CSS + 動畫 |

### 資料流（單向）
```
使用者事件（點擊/切換）
   → 狀態層更新 + 鎖定按鈕
   → 引擎層決定結果（純隨機，先算後播）
   → 視圖層播放動畫 → 落定
   → 視圖層依據引擎結果渲染結果區
   → 狀態層解鎖
```

### 核心鐵則
- **先決定結果，再播放動畫**：擲骰瞬間就以 `Math.random()` 決定每粒點數，動畫僅是把骰子「轉到」既定的面。保證動畫結束時顯示與結果一致（對應 AC-4）。
- **動畫期間鎖定**：`isRolling` 旗標為 true 時，所有擲骰相關按鈕停用（對應 AC-5）。
- **音效在手勢內初始化**：首次點擊的同一事件循環內呼叫 `AudioContext.resume()`（對應 AC-14）。

---

## 2. 全域狀態設計

單一狀態物件，包含：

| 欄位 | 型別 | 說明 |
|---|---|---|
| `mode` | `'free' \| 'bet' \| 'battle'` | 目前模式 |
| `diceCount` | `1～3` | 骰子數量（跨模式保留） |
| `isRolling` | `boolean` | 動畫鎖定旗標 |
| `betChoice` | `'big' \| 'small' \| null` | 猜大小模式的選擇 |
| `records.free` | `[]` | 純擲骰歷史（會話內） |
| `records.bet` | `{ win, lose, push }` | 猜大小勝／敗／和局 |
| `records.battle` | `{ gameWin, gameLose, playerWin, cpuWin }` | 對戰：`playerWin/cpuWin`＝目前賽事局分（0～2），`gameWin/gameLose`＝賽事勝敗（勝率＝gameWin ÷ 總賽事） |
| `soundOn` | `boolean` | 音效開關 |

狀態更新一律透過小型函式（如 `setState`）統一進行，避免分散修改。

---

## 3. 骰子引擎（結果產生與判定）

### 3.1 擲骰結果
- 呼叫一次 = 產生 `diceCount` 顆介於 1～6 的整數（`Math.floor(Math.random()*6)+1`）。
- 總和 = 各點數相加。
- 隨機性不須密碼學等級（NFR-6），不需「手動搖骰」的物理模擬——動畫的搖晃只是視覺效果。

### 3.2 骰子面與旋轉的對照（決定「轉到哪」）
每粒骰子為 CSS 立方體。定義「面編號 → 目標旋轉」靜態對照表，動畫以**起始隨機旋轉 → 目標旋轉**的方式收斂，因為模組化（旋轉 360° 的整數倍）不會改變最終面，目標角度可加 N 圈旋轉以增加翻轉效果。

實作採用之對照表（已驗證正確，勿再以數學推算）：

| 點數 | 立方體上位置 | 目標旋轉（動畫結束姿態） |
|---|---|---|
| 1 | 正面（front） | `rotateX(0) rotateY(0)` |
| 2 | 頂面（top） | `rotateX(-90deg)` |
| 3 | 右面（right） | `rotateY(-90deg)` |
| 4 | 左面（left） | `rotateY(90deg)` |
| 5 | 底面（bottom） | `rotateX(90deg)` |
| 6 | 背面（back） | `rotateY(180deg)` |

各面定位（靜態 CSS）：正面 `rotateY(0)`、右面 `rotateY(90deg)`、左面 `rotateY(-90deg)`、頂面 `rotateX(90deg)`、底面 `rotateX(-90deg)`、背面 `rotateY(180deg)`，皆再 `translateZ(邊長/2)`。

### 3.3 勝負判定（猜大小）

判定順序固定為：**特殊規則 → 和局 → 大小比較**，使用每骰數的臨界值表：

| 骰數 | 小區間 | 大區間 | 和局 | 特殊 |
|---|---|---|---|---|
| 1 | 總和 1～3 | 總和 4～6 | 無 | 無 |
| 2 | 總和 2～6 | 總和 8～12 | 總和 7 | 無 |
| 3 | 總和 4～10 | 總和 11～17 | 無 | 三粒同點 → 通殺（必輸） |

判定流程（描述）：
1. 若骰數 = 3 且三粒點數相同 → 結果「豹子」，押大押小皆輸，戰績 +1 敗。
2. 若骰數 = 2 且總和 = 7 → 結果「和局」，場次不計入勝敗。
3. 否則以總和與臨界值比較：`小區間上限 = floor(3.5 × 骰數)`，總和 ≤ 上限為小，否則為大；與玩家下注相符 → 勝，否則 → 敗。

### 3.4 勝負判定（對戰）
- 每局產生兩組結果（玩家組、電腦組，骰數相同）。
- 玩家總和 > 電腦總和 → 玩家 +1 局；反之電腦 +1 局；相等 → 和局。
- **平手自動重擲**（實作定案）：顯示「平手！自動重擲…」→ 延遲約 700ms → 自動再擲雙方（重播搖骰音效）→ 直到分出勝負；重擲期間維持 `isRolling` 鎖定，比分不變。以遞迴 Promise 串接實作，平手機率低（對稱隨機），不會造成實務上的無限迴圈。
- 任一比分達 2 → 賽事結束，宣告勝者，顯示「再來一場」。
- 勝率 = 賽事勝場 ÷ 賽事總數（會話內累計）。

---

## 4. 3D 骰子實作

### 4.1 立方體構成
- 骰子容器：`position: relative` + `transform-style: preserve-3d`，尺寸以 CSS 變數（如 `--die-size`）控制，方便響應式縮放。
- 六個面：`position: absolute`，各面以「旋轉 + `translateZ(邊長/2)`」定位到立方體表面。
- 容器外層包一層 `perspective` 容器，供使用者以旋轉角度檢視立體感。

### 4.2 點數排版
- 每個面內部用固定的 3×3 定位網格（9 個可能位置），點數 1～6 各自對應位置組合：

| 點數 | 位置 |
|---|---|
| 1 | 中心 |
| 2 | 左上、右下 |
| 3 | 左上、中心、右下 |
| 4 | 四角 |
| 5 | 四角＋中心 |
| 6 | 左右各三（上中下） |

- 用 `data-*` 屬性（如 `data-face="5"`）讓 CSS 依點數套用點位，避免 JS 拼 DOM。

### 4.3 視覺細節（深色主題）
- 骰面以淺色（白／象牙）＋深色圓點，在深色背景上對比清楚；圓點用圓角 div 而非偽元素擴充。
- 面與面交界以微漸層模擬光影（線性漸層 + `box-shadow`），不引入貼圖。

---

## 5. 動畫流程

### 5.1 擲骰時間軸
1. 點擊「擲骰」→ 立即決定全部結果（引擎層）。
2. 設定 `isRolling = true`，停用擲骰按鈕。
3. 每粒骰子套用動畫：隨機起始旋轉 → 快速震盪（translate 微幅隨機抖動）→ 收斂至目標旋轉。
4. 多粒骰子以 `stagger`（每粒延遲約 80～120ms）依序落定。
5. 總時長 0.8～1.2 秒，以最後一粒的動畫結束事件（`transitionend` / WAAPI `finished`）作為統一完成點。
6. 完成 → 解鎖按鈕 → 渲染結果區。

### 5.2 動畫技術選擇
- 首選 **Web Animations API（`element.animate`）**：可程式化控制 keyframes、延遲、結束 Promise，且不使用 100% CPU 的連續 requestAnimationFrame 迴圈。
- **重要**：動畫必須設定 `fill: 'forwards'`，並在 `finished` 後再次套用最終 transform（雙保險）。否則動畫結束瞬間骰子會彈回預設面（正面 1），造成「永遠擲出 1 點」的錯誤。
- 備案：CSS transition + `transitionend`（對舊瀏覽器更穩，但需管理結束事件）。
- 只動畫 `transform` 與 `opacity`（NFR-2）。

### 5.3 減少動效（`prefers-reduced-motion`）
- 偵測到使用者偏好「減少動效」→ 跳過震盪與翻轉動畫，直接把骰子套用目標旋轉（瞬間落定），功能完全不受影響（對應補充驗收第 3 條）。

---

## 6. 三模式互動邏輯

| 模式 | 互動流程 | 結果區顯示 |
|---|---|---|
| 純擲骰 | 選骰數 → 擲骰 | 每粒點數、總和、總和範圍 |
| 猜大小 | 選骰數 → 選大／小 → 擲骰 | 點數、總和、勝／敗／和局、規則摘要 |
| 對戰 | 選骰數 → 擲骰（玩家先落定，電腦後落定） | 雙方點數與總和、本局勝者、比分、賽事勝者 |

### 猜大小的互動約束
- 未選大／小時，擲骰按鈕停用，並以文字提示「請先選擇大或小」（對應 AC-6）。
- 規則摘要隨骰數動態更新（1 粒、2 粒含和局 7、3 粒含豹子）。

### 對戰的互動約束
- 玩家骰與電腦骰分組呈現（左右兩區），玩家先落定、電腦接續落定，視覺節奏明確。
- 平手時自動重擲（見 §3.4），無須玩家介入；重擲前後比分不變。
- 賽事結束後，擲骰按鈕轉為「再來一場」；點擊後比分歸零、勝率保留（沿用 requirements 第 9 節預設）。

### 模式切換
- 切換分頁即切換視圖區塊（純顯示切換，不重建骰子引擎）。
- **切換模式時必須重建骰子 DOM（`renderDice`）**：對戰模式需要「你／電腦」雙組，其餘模式單組；未重建會導致對戰時抓不到骰子而拋錯。
- 各模式戰績分開記錄、分開顯示。
- 動畫進行中禁止切換模式（或切換時先中止並還原狀態，二擇一，建議前者較簡單）。

---

## 7. 音效合成（Web Audio）

### 7.1 結構
- 單一 `AudioContext` 實例（lazy 建立，首次手勢內 `resume()`）。
- 共用 master 增益節點 → 接到輸出；開關 = 切換 master 增益為 0。

### 7.2 音效設計
| 音效 | 合成方式 |
|---|---|
| 搖骰聲 | 一串（約 8～12 個）短促白噪聲脈衝：`BufferSource`（預先產生的 20～40ms 噪聲 buffer）+ 帶通濾波器，間隔隨機化（模擬骰子碰撞） |
| 落定聲 | 單發短促噪聲 tick ＋ 低頻衰減正弦（模擬骰子敲擊桌面） |

- 所有 buffer 於頁面載入時（或首次手勢後）預先產生，避免擲骰瞬間產生 buffer 造成卡頓。
- 播放失敗（例如瀏覽器政策）時靜默忽略，不影響遊戲（降級處理）。

---

## 8. UI 結構與事件

### 8.1 版面區塊（自上而下）
```
標題列（含音效開關）
模式分頁（純擲骰｜猜大小｜對戰）
骰數控制（− 目前骰數 ＋，範圍 1～3）
模式專屬控制區（猜大小：大／小選擇；對戰：計分板）
骰子舞台（骰子渲染區）
結果區（點數、總和、判定、規則摘要、戰績）
主要動作按鈕（擲骰／再來一場）
```

### 8.2 事件設計
- 所有按鈕使用 `click`（行動裝置自動支援）；關鍵區域設 `touch-action: manipulation` 移除點擊延遲。
- **注意**：使用 `hidden` 屬性做顯示切換的區塊（猜大小下注、對戰計分板），必須附加 CSS `[hidden] { display: none }` 規則，否則 `display: flex` 會覆蓋隱藏。
- 事件處理統一先檢查 `isRolling`，鎖定期間直接忽略（防連點）。
- 骰數控制：`−`／`＋` 按鈕，達到 1／3 邊界時停用對應按鈕（對應 AC-2）。
- 可及性：按鈕提供 `aria-label`（如「擲骰」）；結果區用文字輸出，不只依賴顏色（NFR-4）。

---

## 9. 響應式策略

- **骰子尺寸**：以 CSS 變數 + `clamp()` 計算（如 `--die-size: clamp(64px, 22vw, 96px)`），三粒骰子用 flex 排版自動縮放／換行，窄螢幕不溢出（對應 AC-17）。
- 舞台與結果區最大寬度限制（`max-width: 480px` 並置中，桌機 ≥720px 時放寬至 560px），桌機不鋪滿全寬。
- 版面基準線以視窗高度（`dvh`）計算，避免手機網址列伸縮造成跳動。
- 安全區：`env(safe-area-inset-*)` 處理瀏海螢幕（次要，可後補）。
- 驗證：iPhone Safari／Android Chrome 直式與橫式皆需人工檢查（AC-16）。

---

## 10. 邊界情況與防呆

| 情境 | 處理 |
|---|---|
| 動畫中連點擲骰 | `isRolling` 鎖定，直接忽略 |
| 動畫中切換骰數 | 控制鈕停用（與擲骰按鈕同鎖） |
| 動畫中切換模式 | 分頁停用，或先中斷動畫並還原，建議採「停用」 |
| 對戰平手自動重擲期間連點 | 維持 `isRolling` 鎖定，直接忽略 |
| 切換模式後骰子組未重建 | 於 `setMode` 內呼叫 `renderDice` 重建（對戰雙組／其餘單組） |
| 動畫結束骰子彈回預設面 | 動畫加 `fill:'forwards'`＋完成後再套用最終 transform（見 §5.2） |
| 猜大小未選擇就擲 | 按鈕停用（前端先擋，不依賴後端） |
| AudioContext 被瀏覽器阻擋 | 靜默失敗，遊戲照常 |
| 平手局（2 粒總和 7／對戰同總和） | 猜大小：和局不計；對戰：自動重擲 |
| 系統減少動效 | 跳過動畫直接落定 |
| 快速切換音效開關 | 開關僅控制 master 增益，不中斷已排程音效 |

---

## 11. 與需求的對應關係

| 技術設計 | 對應需求／驗收 |
|---|---|
| 3.1 先算後播 | AC-4、關鍵實作約束 |
| 5.1 動畫鎖定 | AC-5 |
| 3.3 判定表 | AC-6～AC-10 |
| 3.4 對戰邏輯 | AC-11～AC-13 |
| 7 音效合成 | AC-14、AC-15 |
| 9 響應式 | AC-16、AC-17 |
| 4／5 純 CSS 3D、WAAPI | NFR-1、NFR-2、NFR-3 |
| 5.3 減少動效 | NFR-5 |

---

## 12. 驗證方式（實作後）

1. 以 `file://` 開啟、DevTools Network 面板確認僅 1 個請求（離線可用）。
2. 逐條核對第 8 節驗收標準（AC-1～AC-17）＋補充 3 條。
3. 動畫流暢度：Performance 面板確認擲骰期間無長任務、維持 60fps。
4. 手機實機（iPhone Safari／Android Chrome）橫直向各測一輪三模式。
